<% @categories.each do |category| %>
  <h3><%= link_to category.name, category_path(category.name) %></h3>
  <ul>
    <% category.articles.each do |article| %>
      <li><%= link_to article.title, article_path(article.category.name, article.url) %></li>
    <% end %>
  </ul>
<% end %>
